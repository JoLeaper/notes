const { execute } = require('./lib/notes');
const { parse, valid } = require('./lib/input');

// console.log(parse(process.argv));

// const thing = valid() 
// console.log(thing);
// execute(thing);
